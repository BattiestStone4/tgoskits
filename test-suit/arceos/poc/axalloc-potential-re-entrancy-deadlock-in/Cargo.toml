[package]
name = "arceos-axalloc-potential-re-entrancy-deadlock-in"
version = "0.1.0"
edition.workspace = true

[dependencies]
axstd = { workspace = true, features = ["alloc"], optional = true }
axalloc = { workspace = true }